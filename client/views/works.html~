<div ng-init="ctrl.lookup()" ng-show="ctrl.artists.works">
  <h3 ng-show="ctrl.artists.works.length">Works for {{ ctrl.artists.name }}</h3>
  <table class="table table-striped"
         ng-show="ctrl.artists.works.length" cellpadding="10">
    <thead>
      <tr>
        <th>ID</th>
        <th>Title</th>
        <th>Language</th>
        <th>Type</th>
        <th>Notes</th>
      </tr>
    </thead>
    <tbody>
      <tr ng-repeat="work in ctrl.artists.works">
        <th>{{ work.id }}</a></th>
        <td>{{ work.title }}</th>
        <td>{{ work.language }}</td>
        <td>{{ work.type }}</td>
        <td>{{ work.disambiguation }}</td>
      </tr>
    </tbody>
  </table>
</div>
